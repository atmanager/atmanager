{% extends 'AtBundle::base_at.html.twig' %} 
{% block content %}

    <h4>Técnico trabajando de AT: {{ entity.at.id }}</h4>

    <table class="table">
        <tbody>
            <tr>
                <th>Técnico</th>
                <td>{{ entity.tecnico }}</td>
            </tr>
            <tr>
                <th>Jerarquia</th>
                <td>{{ entity.rol }}</td>
            </tr>
            
        </tbody>
    </table>
   
    <a href="{{ path('atecnico', {'atId': entity.at.id }) }}"><h5>Aceptar</h5></a>
    
{% endblock %}
