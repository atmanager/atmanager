{% extends 'BackendBundle::base_backend.html.twig' %}

{% block content %}
    <h3>listado de fallas</h3>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombre</th>
                <th>Descripamplia</th>
                <th>Estado</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('falla_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.nombre }}</td>
                <td>{{ entity.descripamplia }}</td>
                <td>{{ entity.estado }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('falla_show', { 'id': entity.id }) }}">Ver</a>
                         <a href="{{ path('falla_edit', { 'id': entity.id }) }}">Editar</a>
                    </li>
                    <li>
                       
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('falla_new') }}">
                Crear nueva Falla
            </a>
        </li>
    </ul>
    {% endblock %}
