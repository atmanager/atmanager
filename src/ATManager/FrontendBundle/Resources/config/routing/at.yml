at_listado:
    pattern:  /
    defaults: { _controller: FrontendBundle:At:index }

at_nuevo:
    pattern:  /new
    defaults: { _controller: FrontendBundle:At:new }

at_editar:
    pattern:  /edit/{id}
    defaults: { _controller: FrontendBundle:At:edit }    

at_eliminar:
    pattern:  /delete/{id}
    defaults: { _controller: FrontendBundle:At:delete }
